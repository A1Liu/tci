use crate::buckets::*;
use crate::lexer::*;
use crate::new_ast::*;
// use crate::util::*;

pub fn parse<'a>(buckets: BucketListRef<'a>, toks: &[Token]) -> &'a [GlobalStatement<'a>] {
    return &[];
}
